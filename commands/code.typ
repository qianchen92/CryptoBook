#import "../commands/font.typ": *
#import "../commands/common.typ": *
#let to = $arrow$
#let gets = $arrow.l$
//#let getsr = $attach(<-, t: "$")$
#let getsr = gets
#let negl = algfont[negl($secpar$)]
#let Unif(x) = algfont[U] + "(" + x + ")"
